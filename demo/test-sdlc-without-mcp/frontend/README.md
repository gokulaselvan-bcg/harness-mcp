# Task Manager Frontend

Vite + React + TypeScript UI that talks to the FastAPI backend.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

App runs at http://localhost:5173. The backend must be running at http://localhost:8000 (override with `VITE_API_URL`).

## Build

```bash
npm run build
```
